{
  plugins.bacon = {
    enable = true;
  };

  plugins.rustaceanvim = {
    enable = false;
  };
  plugins.crates-nvim = {
    enable = true;
  };

  plugins.rust-tools = {
    enable = true;
  };
}
